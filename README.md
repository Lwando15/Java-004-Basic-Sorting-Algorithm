# Java-004-Basic-Sorting-Algorithm
This Java program compares the performance of **Bubble Sort, Selection Sort, and Insertion Sort**. It generates random arrays of different sizes, sorts them using each algorithm, and measures the execution time. The program then calculates and displays the **average sorting time in milliseconds** for each algorithm.
